# Supabase (public — safe for client bundle)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Supabase edge functions (server-side only, never in client bundle)
SUPABASE_SERVICE_ROLE_KEY=
# Free key from https://aistudio.google.com/apikey — used to grade text/url
# assignment submissions (evaluate-submission edge function).
GEMINI_API_KEY=
RESEND_API_KEY=
RESEND_FROM_EMAIL=

# App
SITE_URL=http://localhost:5173
